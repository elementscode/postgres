{
  "name": "@elements/sql",
  "description": "Work with sql databases in your app.",
  "version": "0.0.0",
  "license": "SEE LICENSE IN LICENSE.txt",
  "browser": {
    "./database": false,
    "./pool": false,
    "./shared_pool": false
  },
  "engines": {},
  "dependencies": {
    "pg": ">=8.3.0 <8.4.0"
  },
  "devDependencies": {
    "@types/pg": "^7.14.4"
  },
  "peerDependencies": {},
  "scripts": {
    "clean": "rm -rf .elements/dist",
    "copyfiles": "mkdir -p .elements/dist && cp README.md LICENSE.txt package.json .elements/dist",
    "build": "yarn run clean && yarn run copyfiles && tsc"
  }
}
